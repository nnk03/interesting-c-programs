# empty-vs-void

With the empty notation
`int foo()`

If we pass, `foo(12)`, some compilers just give a warning

But with the void notation
`int foo(void)`

All compilers enforce `foo` to take zero arguments
